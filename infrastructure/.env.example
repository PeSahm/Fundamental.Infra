# =============================================================================
# Environment Variables for Terraform/Terragrunt
# =============================================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit the actual .env file to version control!
# 
# Usage: source .env && cd live/production && terragrunt run-all apply
# =============================================================================

# -----------------------------------------------------------------------------
# Cloudflare Configuration
# -----------------------------------------------------------------------------
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone:DNS:Edit (scoped to your zone)
export CLOUDFLARE_API_TOKEN="your-cloudflare-api-token-here"

# Get your Zone ID from the Cloudflare dashboard (right sidebar on zone overview)
export CLOUDFLARE_ZONE_ID="your-cloudflare-zone-id-here"

# -----------------------------------------------------------------------------
# GitHub Configuration
# -----------------------------------------------------------------------------
# Create a Personal Access Token from: https://github.com/settings/tokens
# Required permissions (Fine-grained):
#   - Repository: Actions (RW), Environments (RW), Metadata (R), Secrets (RW)
# Or (Classic):
#   - repo, admin:repo_hook
export GITHUB_TOKEN="your-github-personal-access-token-here"

# -----------------------------------------------------------------------------
# SSH Configuration for Deployment
# -----------------------------------------------------------------------------
# Option 1: Plain text (works but newlines can be tricky)
# export SSH_PRIVATE_KEY=$(cat ~/.ssh/deploy_key)

# Option 2: Base64 encoded (recommended for safety)
# export SSH_PRIVATE_KEY=$(cat ~/.ssh/deploy_key | base64 -w 0)

# Placeholder - replace with your actual key
export SSH_PRIVATE_KEY="your-base64-encoded-ssh-private-key-or-raw-key"

# -----------------------------------------------------------------------------
# Container Registry Configuration
# -----------------------------------------------------------------------------
# Credentials for the private container registry at registry.academind.ir
# These are injected into GitHub Actions secrets for CI/CD pipelines
export REGISTRY_USER="your-registry-username"
export REGISTRY_PASSWORD="your-registry-password"

# -----------------------------------------------------------------------------
# Optional: Terraform Logging
# -----------------------------------------------------------------------------
# export TF_LOG=INFO
# export TF_LOG_PATH=./terraform.log
